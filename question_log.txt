2026.3.24.22:52
1.已完成工作
(1)完成项目基础架构开发：
    Excel 结构化数据加载、方案一（字段级加权嵌入）、方案二（全文本拼接嵌入）、FAISS 向量库构建、交互式检索 demo，实现了基础的「方言词查释义」功能。
    完成方案一初步优化：调整字段权重、发音字段语义拼接、基础关键词过滤，但核心匹配痛点未完全解决。
2.现存核心问题
    同义语义匹配失效：无法完成普通话同义词与方言释义的语义映射（如用户输入 “爸爸”，无法匹配到释义含 “父亲” 的方言词 “郎罢”），为当前最核心痛点。
    自然语言意图理解不足：无法处理用户口语化、长句式查询（如 “我今天没吃饭中的‘吃饭’用方言怎么说”），无法精准提取核心查询对象。
    双向查词能力失衡：仅「方言词查释义」效果达标，「普通话查对应方言词」的匹配率极低，未实现项目核心需求。
    模糊匹配泛化能力弱：仅能匹配文本字面内容，无法理解用户描述性、定义式的查询，场景化查询命中率低。

方案一二均无法完全满足要求，准备采用
借鉴结构化知识库增强 + 检索链路优化的成熟方案，
以 **「离线一次性增强、在线轻量推理」** 为核心，避开高算力全量微调，完全兼容现有项目架构，做增量式升级。
2026.4.4
1.已完成工作
(1)完成项目检索功能开发：
    借鉴结构化知识库增强 + 检索链路优化的成熟方案，以 **「离线一次性增强、在线轻量推理」** 为核心，避开高算力全量微调，完全兼容现有项目架构，做增量式升级
    利用这个路线和LLM配合已完成阶段二的目标，实现了方言，普通话释义，用户的模糊查询返回结构化字段
2.现存问题
    针对方案三中问题有：
    （1）标准的IPA查询没有做到匹配(todo)
    （2）对于语音检索那块的传来的IPA因为口音与发音问题（儿化等等），IPA不是很标准化，如何做到IPA的模糊查询匹配
    （3）对于拼音模糊匹配目标，由于用户理解与发音的不同（前鼻后鼻音等等）,现在有两个路线：
        a.一是直接分析有误差的拼音与数据语料中的拼音进行模糊匹配
        b.将误差拼音转化为IPA（参考已有的输入法项目和莆仙话发音与拼音转化规范），按照IPA模糊查询匹配
    （4）针对（2）（3）要么分开路线进行双路匹配，要么统一按照IPA进行查询，所有模糊匹配路线未确定(todo)